# 图书馆智能问答系统开发文档更新（2026.4.29）


---

## 1. 知识图谱本体升级

为了支持从单纯的“找书”跨越到“探索领域大牛及其学术关系”，底层图谱的数据模式进行了如下扩展：

### 1.1 节点属性增强
* **Author (作者)** 节点新增核心属性：
  * `bio`: 作者生平及详细介绍。
  * `primary_field`: 作者的核心研究或创作领域。

### 1.2 关系类型扩展
新增三种作者间的横向关联关系，用于构建学术脉络网络：
* `[:INFLUENCED_BY]`: 学术传承或思想影响。
* `[:WORKS_WITH]`: 合作关系（如同属一个研究团队或合著）。
* `[:IN_SAME_FIELD]`: 属于同一研究流派或学术圈。

---

## 2. 数据源与导入逻辑更新 (kg_module)

### 2.1 新增基础数据文件
在 `kg_module/raw_data/` 目录下新增两份结构化数据文件：
1. **authors.csv**: 维护作者详情（包含 `name`, `primary_field`, `bio` 字段）。
2. **author_relations.csv**: 维护作者间关系网络（包含 `source_author`, `target_author`, `relation_type` 字段）。

### 2.2 导入脚本更新 (import_data.py)
在原有导入脚本中追加 `import_extended_data()` 逻辑，实现新节点的合并与新关系的动态写入：
* 使用 `MERGE (a:Author {name: $name}) SET ...` 更新作者节点属性。
* 使用字符串拼接动态生成关系类型的 Cypher 语句，执行节点间的网络连线。

---

## 3. 后端服务模块更新 (actions)

### 3.1 数据库连接器扩展 (neo4j_connector.py)
* **新增 `get_author_profile(author_name)` 方法**:
  * 功能：一键聚合作者的基本信息、名下的所有馆藏书籍，以及与其存在连线的其他历史人物关系。
  * Cypher 核心语法：使用双重 `OPTIONAL MATCH` 确保即使某作者没有书籍或没有关联人物，也能返回基础生平。
* **修改 `get_books_by_topic(topic_name)` 方法**:
  * 优化：在按主题查书的结果中，使用 `OPTIONAL MATCH (b)-[:WRITTEN_BY]->(a:Author)` 连带查询出作者姓名，提升前端展示的信息丰富度。

### 3.2 大模型生成模块扩展 (llm_server.py)
* **新增 `generate_author_reply(author_name, kg_context)` 方法**:
  * 功能：针对作者查询场景的特化 Prompt 工程。
  * 提示词策略：强制 LLM 关注传入参数中的 `connections` (学术/人物关联) 字段，要求模型以“学术脉络”或“趣闻”的形式将复杂的人物图谱关系转化为自然、专业的讲解文本。

### 3.3 动作调度中心更新 (actions.py)
* **新增 `ActionIntroduceAuthor` 类**:
  * 职责：承接 Rasa 识别到的作者查询意图。
  * 流程：提取 `author` 槽位 -> 调用 `get_author_profile` 查询图谱 -> 传入 `generate_author_reply` 生成文本 -> 返回至前端。
  * 状态控制：在 `run` 方法结束前强制返回 `[SlotSet("author", None)]`，清空对话记忆，防止上下文污染。

---

## 4. 意图识别与对话管理配置更新 (Rasa Core/NLU)

为使 Rasa 能够听懂并分发关于“作者”的指令，全局配置进行了以下同步：

### 4.1 领域定义 (domain.yml)
* **实体与槽位**: 注册 `author` 实体及对应的同名文本槽位。
* **意图与动作**: 声明新的意图 `introduce_author` 与自定义动作 `action_introduce_author`。

### 4.2 NLU 训练语料 (data/nlu.yml)
在数据集中新增 `introduce_author` 意图，提供包含作者实体的高质量自然语言泛化语料，例如：
* "给我介绍一下[威廉·吉布森](author)"
* "[李航](author)是谁"

### 4.3 对话规则 (data/rules.yml)
新增固定映射规则，确保一旦 NLU 引擎识别到 `introduce_author` 意图，对话管理器 100% 触发 `action_introduce_author`。

---

## 5. 部署与编译指南

由于本次更新涉及图谱底层结构及 NLP 模型的双重变动，请按以下顺序执行重启流程：

1. **更新数据库**:
   进入虚拟环境，运行 `python kg_module/import_data.py` 以合并最新的作者及关系数据至 Neo4j。
2. **重新编译 Rasa 模型**:
   在项目根目录运行 `rasa train`，使新增的意图语料生效。
3. **重启 Action 服务**:
   重启运行在 5055 端口的动作服务器 (`rasa run actions`)，加载最新的 Python 业务代码。
4. **验证功能**:
   启动 Rasa 主服务后，在前端或命令行输入“介绍一下伊恩·古德费洛”，检查是否能够输出包含研究领域、关联书籍及人物关系网络的综合回复。